#include<iostream>
#include<vector>
using namespace std;

class Solution {
public:
	vector<string> result;

	void GetIp(string& s, vector<string>& tmp, int index, int num)
	{
		if (num == 4 || index >= s.size())
		{

		}
	}

	vector<string> restoreIpAddresses(string s) {
		if (s.size() < 4) return result;
		vector<string> tmp;

	}
};